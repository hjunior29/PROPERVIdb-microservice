{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Go: Start",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "cwd": "${workspaceFolder}",
            "program": "${workspaceFolder}/main.go",
            "envFile": "${workspaceFolder}/.env",
            "console": "integratedTerminal"
        }

    ]
}